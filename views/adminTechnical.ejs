<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Technical Dashboard</title>
  </head>
  <body>
    <form action="/admin/technical/createTechnicalQuestion" method="post">
      <input
        type="text"
        placeholder="Add a new technical question"
        name="addTechnicalQuestion"
      />
      <input
        type="text"
        placeholder="Add explanation"
        name="explanationAnswer"
      />
      <input type="text" placeholder="The use" name="useAnswer" />
      <input type="text" placeholder="Add an example" name="exampleAnswer" />
      <input
        type="url"
        placeholder="Add a link for reference"
        name="linkAnswer"
      />
      <button type="submit">Submit</button>

      <ul>
        <!-- technicals here is the object name we've set in adminTechnicalController -->
        <% technicals.forEach( el => { %>
        <li class="todoItem" data-id="<%=el._id%>">
          <span><%= el.technicalQuestion %></span>
          <!-- technicalQuestion here is the object key of our document in the db -->
          <span class="del"> Delete </span>
        </li>
        <% }) %>
      </ul>
    </form>
  </body>
</html>
